import React from "react";
import ReactDOM from "react-dom/client";
import { Main } from "./main"; // ajusta la ruta si es necesario

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Main />
  </React.StrictMode>
);
